// 时间格式格式化
function format(oTime) {
  date = getDate(oTime);
  var year = date.getFullYear()
  var month = date.getMonth() + 1
  var day = date.getDate()
  var mat = year + '-' + month + '-' + day
  return mat
}
module.exports = {
  "format": format
}